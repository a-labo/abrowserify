/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 */

'use strict'

const abrowserify = require('./abrowserify')

let lib = abrowserify.bind(this)

Object.assign(lib, abrowserify, {
  abrowserify
})

module.exports = lib